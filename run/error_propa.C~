// error propagation for FILFO efficiency
int error_propa() {

  const double x = 0.;
  const double y = 0.;
  
  double z = 2 * x / (y + 2 * x);

  cout << "x = " << x << "\n"
       << "y = " << y << "\n"
       << "z = " << z << endl; 


  return 0;
}
